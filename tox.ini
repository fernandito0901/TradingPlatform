[tox]
envlist = smoke

[testenv:smoke]
skipsdist = true
allowlist_externals = bash
commands_pre =
    bash -c 'if ! command -v docker >/dev/null; then echo "SKIPPED – Docker not available" && exit 0; fi'
commands = bash scripts/docker_smoke.sh --ci
